@model ToDoList.Models.LoginViewModel

<!DOCTYPE html>
<html>
<head>
    <title>Login</title>

    <style>
        body {
            background-color: #F6F5F2;
            font-family:'Century Schoolbook'
        }

        .container {
            max-width: 400px;
            margin: 0 auto;
            padding: 40px 20px;
            background-color: #FFEFEF;
            border-radius: 5px;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
        }

        label {
            font-weight: bold;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            background-color: #F0EBE3;
        }

        button[type="submit"] {
            width: 100%;
            padding: 10px;
            background-color: #F3D0D7;
            color: #333;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

            button[type="submit"]:hover {
                background-color: #F3D0D7;
                opacity: 0.8;
            }

        p {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
        }

        a {
            color: #F3D0D7;
            text-decoration: none;
        }

            a:hover {
                text-decoration: underline;
            }
    </style>
</head>
<body>
    <div class="container">
        <h2>Login</h2>
        <form asp-action="Login" method="post">
            <div>
                <label for="username">Username:</label>
                <input type="text" id="username" name="Username" value="@Model?.Username" required />
            </div>
            <div>
                <label for="password">Password:</label>
                <input type="password" id="password" name="Password" required />
            </div>
            <button type="submit">Login</button>
        </form>
        <!-- Optional: Add a link to the registration page if you want users to be able to register -->
        <p>Don't have an account? <a href="@Url.Action("Register", "Account")">Register here</a>.</p>
    </div>
</body>
</html>